language: node_js
script: npm run build
deploy:
  provider: pages
  github-token: $GITHUB_TOKEN
  local-dir: build
  target-branch: gh-pages
  keep-history: tru
  skip-cleanup: true
  verbose: true
  on:
    branch: master
notifications:
  slack: $SLACK_NOTIFICATIONS_CODE
